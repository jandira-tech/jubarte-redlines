{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core events + native open/save dialogs",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
